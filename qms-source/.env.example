# Backend environment variables (BackEnd/.env)
# Copy this file to `BackEnd/.env` (or set vars in your environment) and fill values for local/dev/production as appropriate.

# Database
DB_NAME=your_db_name
DB_USER=your_db_user
DB_PASS=your_db_password
DB_HOST=localhost

# App
PORT=5000
NODE_ENV=development

# JWT
JWT_SECRET=replace_this_with_a_strong_secret

# Mail (used by bootstrap and email notifications)
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USER=your_smtp_user
MAIL_PASS=your_smtp_password
MAIL_FROM=admin@example.com

# Bootstrap
# When true (and DB is empty) the app will try to create default company/role/user and may send email
BOOTSTRAP_ON_SYNC=true

# Frontend: set VITE_BASE_API_URL for the Vite app
VITE_BASE_API_URL=http://localhost:5000/api

## Notes
- For local development you can set `MAIL_*` to empty values; the app will attempt to send email only if SMTP is configured (see docs/architecture.md for recommended gating).
- For production use a secure `JWT_SECRET` and a secure SMTP provider. Do NOT commit `.env` to source control.
